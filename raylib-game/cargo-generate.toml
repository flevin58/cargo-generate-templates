[template]
cargo_generate_version = ">=0.9.0"

# General Placeholders
[placeholders]
gh_username = {type = "string", prompt = "What is the github username to publish under?"}
description = {type = "string", prompt = "Provide a brief description of your game", default = "A game made with Rust and Raylib"}
