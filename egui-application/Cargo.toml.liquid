[package]
name = "{{crate_name}}"
version = "0.1.0"
edition = "2024"
authors = ["{{authors}}"]
publish = false
repository = "https://github.com/{{gh_username}}/{{crate_name}}"
description = "{{description}}"
license = "MIT"

[dependencies]
eframe = { version = "0.36.1", features = ["default"] }
image = { version = "0.25", features = ["png"] }
